schema: "schema.graphql"
# documents: null
generates:
  src/interfaces/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      federation: true
      useIndexSignature: true
